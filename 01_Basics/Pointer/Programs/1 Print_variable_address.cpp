#include <iostream>
using namespace std;

int main () {
    // An integer variable 'a'
    int a = 20;

    // Printing the address (hexa) of an integer variable 'a'
    cout << "Address of 'a' = " << &a << endl;

    return 0;
}